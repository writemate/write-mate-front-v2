'use client';
import { HeaderContainer } from "@/styles/workspace/Header.styles";
import Link from "next/link";
import { useLogin } from "@/stores/useLogin";

export default function Header() {
    return (
        <HeaderContainer>

        </HeaderContainer>
    );
}
