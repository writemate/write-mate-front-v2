import { colorSystem } from "@/styles/colorSystem";

export default function Chapter() {
  return (
    <div
      style={{
        borderRadius: "10px",
        border: `1px solid ${colorSystem.orange400}`,
        width: "100%",
      }}
    >
      hi
    </div>
  );
}
